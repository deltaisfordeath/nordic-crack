# QZ Companion sends raw logcat UDP datagrams to this port on the receiving machine
UDP_PORT=8002

# Port this relay server listens on (browser connects here)
PORT=3001

# Set to "true" to test without a treadmill
SIMULATE=false
